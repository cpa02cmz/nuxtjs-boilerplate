import { defineNuxtRouteMiddleware } from '#app'

export default defineNuxtRouteMiddleware((_to, _from) => {
  // This middleware can be used to protect API key pages if needed
  // Currently it's just a placeholder for future implementation
})
