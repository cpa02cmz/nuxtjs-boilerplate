// ============================================
// CONFIG INDEX
// Centralized exports for all configuration modules
// ============================================

// App configuration (site metadata, SEO, URLs)
export * from './app.config'

// Rate limiting configuration
export * from './rate-limit.config'

// Cache configuration (TTL, limits, cleanup)
export * from './cache.config'

// Validation configuration (limits, patterns, messages)
export * from './validation.config'

// UI configuration (animations, z-index, spacing)
export * from './ui.config'

// Timing configuration (timeouts, intervals, delays)
export * from './timing.config'

// Error and success messages
export * from './messages.config'

// HTTP status codes
export * from './http.config'

// Performance configuration
export * from './performance.config'
