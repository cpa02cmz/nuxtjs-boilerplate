# =============================================================================
# Nuxt.js Boilerplate - Environment Configuration
# =============================================================================
# Copy this file to .env and update the values for your environment
# DO NOT commit .env to version control (it's in .gitignore)
# =============================================================================

# =============================================================================
# REQUIRED: Database Configuration
# =============================================================================
# SQLite database file path
DATABASE_URL="file:./data/dev.db"

# =============================================================================
# REQUIRED: Site Configuration (for SEO, canonical URLs, sitemaps)
# =============================================================================
# Primary site URL - used for SEO metadata, sitemap generation, Open Graph tags
NUXT_PUBLIC_SITE_URL="http://localhost:3000"

# Canonical URL - used for canonical link generation (can be same as SITE_URL)
NUXT_PUBLIC_CANONICAL_URL="http://localhost:3000"

# Alternative: You can use these variables instead (they're checked as fallbacks)
# SITE_URL="http://localhost:3000"
# CANONICAL_URL="http://localhost:3000"

# =============================================================================
# OPTIONAL: Environment
# =============================================================================
# Node environment (development, production, test)
NODE_ENV="development"

# =============================================================================
# OPTIONAL: Redis Configuration (for caching)
# =============================================================================
# Uncomment and configure if you want to use Redis for caching
# REDIS_URL="redis://localhost:6379"

# =============================================================================
# OPTIONAL: Rate Limiting Bypass (for admin/testing)
# =============================================================================
# WARNING: Keep this secret! Used to bypass rate limiting for admin operations
# ADMIN_RATE_LIMIT_BYPASS_KEY="your-secret-bypass-key-here"

# =============================================================================
# OPTIONAL: API Keys
# =============================================================================
# API key for external service integrations (if needed)
# API_KEY="your-api-key-here"

# =============================================================================
# AUTO-CONFIGURED: Hosting Platform Variables (do not set manually)
# =============================================================================
# These are automatically set by hosting platforms:
# - VERCEL_URL: Set by Vercel automatically
# - VERCEL_ENV: Set by Vercel automatically
# VERCEL_URL="your-project.vercel.app"

# =============================================================================
# OPTIONAL: Host Configuration
# =============================================================================
# Host binding (default: 0.0.0.0)
# HOST="0.0.0.0"

# Port (default: 3000)
# PORT=3000

# =============================================================================
# PRODUCTION DEPLOYMENT CHECKLIST
# =============================================================================
# Before deploying to production, ensure:
#
# [ ] Update NUXT_PUBLIC_SITE_URL to your production domain
# [ ] Update NUXT_PUBLIC_CANONICAL_URL to your production domain
# [ ] Set NODE_ENV=production
# [ ] Generate a secure DATABASE_URL for production
# [ ] Set ADMIN_RATE_LIMIT_BYPASS_KEY to a secure random string
# [ ] Configure REDIS_URL if using Redis caching
# [ ] Review all API keys and secrets
#
# =============================================================================
